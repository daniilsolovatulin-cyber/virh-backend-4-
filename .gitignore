# dependencies
node_modules/

# local env
.env
.env.local

# runtime data — regenerated by the server, must never be committed
server/db/*.sqlite
server/db/*.sqlite-wal
server/db/*.sqlite-shm
server/uploads/

# собранные архивы
*.zip

# editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
